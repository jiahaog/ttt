# Tic-Tac-Toe

## Commands

```bash
# first run
$ npm install

# build es6 source files in `/src` to `/lib`
$ npm run build

# watch es6 source files in `/src` to `/lib`
$ npm run watch 

# tests
$ npm test 
```
